# Superseded audits

Prior active audit reports are retained directly where path-safe. Larger inherited audit archives are preserved inside exact package archives under `../package_archives/` and indexed under `../package_snapshot_indexes/`.
