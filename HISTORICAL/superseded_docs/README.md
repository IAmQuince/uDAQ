# Superseded documents

This folder contains selected directly reviewable superseded documents, especially prior active release entries. Deep inherited release-history and docs-archive trees are preserved inside exact package archives under `../package_archives/` and indexed under `../package_snapshot_indexes/` to keep the main package unzip-safe.
