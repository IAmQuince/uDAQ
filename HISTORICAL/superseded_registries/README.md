# Superseded registries

Current active registries live in `ACTIVE/registries/`. Prior registry archives are preserved inside exact package archives under `../package_archives/` and indexed under `../package_snapshot_indexes/` to avoid Windows path-length problems.
