# UniversalDAQ source package

This directory contains product implementation code and local module guardrails only. The current baseline uses `ui` as the implementation-facing package name for the user interface surface while retaining frontend/backend authority language in higher-level architecture documents where useful.
