# Bounded rules spine

This package now hosts the bounded rule skeleton for the current orchestration slice.

Current scope:
- register rule definitions
- evaluate canonical runtime truth
- emit governed command intent
- record recent bounded rule rows

Out of scope:
- rich rule editor UI
- generalized automation DSL
- direct adapter bypass paths
