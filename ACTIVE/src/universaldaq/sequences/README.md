# Bounded sequences spine

This package now hosts the bounded sequence skeleton for the current orchestration slice.

Current scope:
- register sequence definitions
- start bounded sequences
- wait on canonical alarm truth
- emit governed command intent through command admission
- record recent bounded sequence rows

Out of scope:
- rich sequence editor UI
- scheduling
- generalized workflow engine
- direct adapter bypass paths
