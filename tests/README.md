# Tests

This tree holds executable verification assets only. Meta tests cover repo/process guardrails in addition to requirement/invariant declaration hygiene. Contract, scenario, invariant, and integration tests execute real bounded-slice assertions against the typed model and shell/service baseline, and no separate placeholder conformance tree remains in this package.
